export default interface Trait {
        id: number;
        name: string;
        entry?: string;
        entry_parsed?: string;
        section?: string;
        section_id?: number;
        default_order?: number;
}